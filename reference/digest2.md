# Digest R object with source reference removed

Digest R object with source reference removed

## Usage

``` r
digest2(object, ..., keep_source = FALSE)
```

## Arguments

- object, ...:

  passed to [`digest`](https://rdrr.io/pkg/digest/man/digest.html)

- keep_source:

  whether to keep the code that generates the object; default is false

## See also

[`removeSource`](https://rdrr.io/r/utils/removeSource.html)
